const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');function read(file){return fs.readFileSync(path.resolve(__dirname,'..',file),'utf8')}
test('sponsor success view escapes stored sponsor and club fields before innerHTML rendering',()=>{const html=read('sponsor/erfolg.html');for(const field of ['orderNumber','company','clubName','levelName'])assert.match(html,new RegExp('esc\\(o\\.'+field.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')))});
test('public certificate escapes all stored text fields before innerHTML rendering',()=>{const html=read('zertifikat.html');for(const field of ['levelName','company','city','clubName','season'])assert.match(html,new RegExp('esc\\(c\\.'+field.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')))});
