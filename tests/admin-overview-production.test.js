const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const overview=fs.readFileSync(path.resolve(__dirname,'../server/admin/overview.js'),'utf8');const adminLib=require('../api/_lib/admin');
test('protected admin overview is not preview-only',()=>{assert.doesNotMatch(overview,/previewWritesAllowed|PREVIEW_ADMIN_ONLY/);assert.match(overview,/requireAdmin/)});
test('payment exceptions contribute to operations attention until refund succeeds',()=>{const data=adminLib.summarizeAdminData({paymentExceptions:[{id:'a',refundStatus:'request_failed',amount:99},{id:'b',refundStatus:'succeeded',amount:99}]});assert.equal(data.summary.refundExceptions.total,2);assert.equal(data.summary.refundExceptions.open,1);assert.equal(data.summary.attention,1);assert.equal(data.paymentExceptions.length,2)});
