const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const {recordAllowedForEnvironment,syntheticTestForEnvironment}=require('../api/_lib/release');
test('production exposes only records explicitly marked non-synthetic',()=>{assert.equal(recordAllowedForEnvironment({syntheticTest:false},'production'),true);assert.equal(recordAllowedForEnvironment({syntheticTest:true},'production'),false);assert.equal(recordAllowedForEnvironment({},'production'),false)});
test('preview/test may use synthetic and legacy untagged records but not explicit production records',()=>{for(const env of ['preview','test']){assert.equal(recordAllowedForEnvironment({syntheticTest:true},env),true);assert.equal(recordAllowedForEnvironment({},env),true);assert.equal(recordAllowedForEnvironment({syntheticTest:false},env),false)}});
test('write tagging helper is deterministic by environment',()=>{assert.equal(syntheticTestForEnvironment('production'),false);assert.equal(syntheticTestForEnvironment('preview'),true);assert.equal(syntheticTestForEnvironment('test'),true)});
test('critical public and post-payment readers apply environment scoping',()=>{for(const file of ['server/clubs.js','server/schools.js','server/public/club-sponsors.js','server/public/spotlight.js','server/certificates/public.js','api/checkout/status.js','api/family-plus/status.js'])assert.match(fs.readFileSync(path.resolve(__dirname,'..',file),'utf8'),/recordAllowedForEnvironment/,`${file} missing environment record guard`)});
